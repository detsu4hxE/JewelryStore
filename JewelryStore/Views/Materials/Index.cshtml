@model IEnumerable<JewelryStore.Models.Materials>

@{
    ViewData["Title"] = "Материалы";
}

<h1>Материалы</h1>

<p style="font-size: large">
    <a asp-action="Create">Создать новый материал</a>
</p>
<table class="table" style="font-size: large; text-align: center">
    <thead>
        <tr>
            <th>
                Название
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Сведения</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
